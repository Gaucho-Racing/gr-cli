# `gr` -- The Gaucho Racing Command Line Tool

`gr` is a handy tool for doing Gaucho Racing things from your command line. If you find yourself repeating steps for common command line tasks over and over again, consider adding a sub-commmand to `gr`.
